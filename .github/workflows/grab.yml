name: OCI A1 Grab

on:
  workflow_dispatch:
  push:
    branches: [master]
  schedule:
    - cron: '0 */5 * * *'

permissions:
  contents: read
  issues: write

concurrency:
  group: oci-a1-grab
  cancel-in-progress: false

jobs:
  grab:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    steps:
      - name: Install OCI CLI
        run: pip install --quiet oci-cli

      - name: Configure OCI (secrets → ~/.oci)
        env:
          OCI_PRIVATE_KEY: ${{ secrets.OCI_PRIVATE_KEY }}
          OCI_USER: ${{ secrets.OCI_USER_OCID }}
          OCI_TENANCY: ${{ secrets.OCI_TENANCY_OCID }}
          OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_REGION: ${{ secrets.OCI_REGION }}
        run: |
          umask 077
          mkdir -p ~/.oci
          printf '%s\n' "$OCI_PRIVATE_KEY" > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem
          cat > ~/.oci/config <<EOF
          [DEFAULT]
          user=$OCI_USER
          fingerprint=$OCI_FINGERPRINT
          tenancy=$OCI_TENANCY
          region=$OCI_REGION
          key_file=$HOME/.oci/key.pem
          EOF

      - name: Grab — 填滿 4/24（1/6+1/6+2/12），每搶到就開 Issue email 你
        env:
          SUPPRESS_LABEL_WARNING: "True"
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          COMPARTMENT_ID: ${{ secrets.OCI_TENANCY_OCID }}
          SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          AD: ${{ secrets.OCI_AD }}
          SSH_PUB: ${{ secrets.OCI_SSH_PUBLIC_KEY }}
        run: |
          set -u
          FREE_OCPU=4
          notify() {   # $1=標題 $2=內文；bot 開 Issue @你 → GitHub email 你
            echo ">> $1 | $2"
            gh issue create -R "$REPO" --title "$1" --body "@zhujun0511-moi $2" >/dev/null 2>&1 || echo "(issue 開失敗，僅 log)"
          }
          cur_ocpu() {
            local v
            v=$(oci compute instance list -c "$COMPARTMENT_ID" --all \
                --query "sum(data[?\"shape\"=='VM.Standard.A1.Flex' && \"lifecycle-state\"!='TERMINATED' && \"lifecycle-state\"!='TERMINATING'].\"shape-config\".ocpus)" \
                --raw-output 2>/dev/null)
            v=${v%%.*}; case "$v" in ''|*[!0-9]*) v=0 ;; esac; echo "$v"
          }
          IMAGE_ID=$(oci compute image list -c "$COMPARTMENT_ID" \
                --operating-system "Canonical Ubuntu" --operating-system-version "22.04" \
                --shape "VM.Standard.A1.Flex" --sort-by TIMECREATED --sort-order DESC \
                --query 'data[0].id' --raw-output)
          case "$IMAGE_ID" in ocid1.image*) : ;; *) echo "❌ 取映像失敗"; exit 1 ;; esac
          printf '%s\n' "$SSH_PUB" > /tmp/ssh.pub
          END=$(( $(date +%s) + 17400 ))
          i=0
          while [ "$(date +%s)" -lt "$END" ]; do
            USED=$(cur_ocpu)
            if [ "$USED" -ge "$FREE_OCPU" ]; then
              notify "🎉 OCI 免費額度搶滿 ${USED}/${FREE_OCPU} OCPU" "1/6+1/6+2/12 已就位，搶容器停止。"
              exit 0
            fi
            if [ "$USED" -lt 2 ]; then OCPU=1; MEM=6; BOOT=47; else OCPU=2; MEM=12; BOOT=90; fi
            REMAIN=$(( FREE_OCPU - USED ))
            if [ "$OCPU" -gt "$REMAIN" ]; then OCPU=$REMAIN; MEM=$(( OCPU*6 )); fi
            i=$((i+1))
            NAME="mongodb-a1-$(date +%s)"
            if oci compute instance launch \
                --compartment-id "$COMPARTMENT_ID" --availability-domain "$AD" \
                --shape "VM.Standard.A1.Flex" --shape-config "{\"ocpus\":$OCPU,\"memoryInGBs\":$MEM}" \
                --image-id "$IMAGE_ID" --subnet-id "$SUBNET_ID" --assign-public-ip true \
                --display-name "$NAME" --boot-volume-size-in-gbs "$BOOT" \
                --ssh-authorized-keys-file /tmp/ssh.pub >/tmp/out 2>&1; then
              INST_ID=$(grep -oiE 'ocid1\.instance[a-z0-9._-]+' /tmp/out | head -1)
              IP=""
              for k in 1 2 3 4 5 6; do
                IP=$(oci compute instance list-vnics --instance-id "$INST_ID" --query 'data[0]."public-ip"' --raw-output 2>/dev/null || true)
                case "$IP" in [0-9]*) break ;; *) IP=""; sleep 10 ;; esac
              done
              notify "✅ 搶到 OCI VM: ${NAME} (${OCPU}c/${MEM}g)" "累計 $((USED+OCPU))/${FREE_OCPU} OCPU。Public IP: ${IP:-建立中,Console查}。登入: ssh -i id_ed25519 ubuntu@${IP:-<IP>}"
              sleep 45
              continue
            fi
            # 只有真正的設定/權限錯才停；容量/網路瞬斷(RequestException)/限流/暫時5xx 一律重試
            if grep -qiE "NotAuthenticated|NotAuthorizedOrNotFound|InvalidParameter|MissingParameter|CannotParseRequest|InvalidImage|Forbidden|does not exist|can not be blank" /tmp/out; then
              echo "❌ 設定/權限錯誤，停止："; head -3 /tmp/out; exit 1
            else
              echo "[$i] $(date -u +%H:%M:%S) 暫時性(容量/網路)，60s 後重試"
            fi
            sleep 60
          done
          echo "本輪 5h 視窗結束（$(cur_ocpu)/${FREE_OCPU} OCPU），下次排程續搶。"
